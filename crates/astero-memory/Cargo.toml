[package]
name = "astero-memory"
version.workspace = true
edition.workspace = true
publish.workspace = true

# M28: deny except the private mapping/windows_native/platform.rs FFI leaf.
[lints.rust]
unsafe_code = "deny"
[lints.clippy]
all = "warn"
