//! Source-bound candidate identity, names, attributes and relocation use evidence.
mod model;
pub use model::{
    CandidateEvidence, CandidateIdentity, CandidateObservation, NameState, RelocationUse,
};
